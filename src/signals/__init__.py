"""Signal engines and aggregator.

Submodules are imported lazily so callers only pay for what they use.
Example:
    from src.signals.technical import TechnicalSignal
    from src.signals.aggregator import SignalAggregator
"""
